# Photo Browser

This is a simple photo browser app that fetches image data from external API and displays the data on web page.

## How to start

In the project directory, install dependencies by running the command:

### `npm install`

In the project directory, start the application by running the command:

### `npm start`

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.
